<문제 1>
1)purpose of this code
맥스 힙을 구성하는 코드를 만드는 것이다.

2) input and output

random으로 숫자를 입력받고 그에 따라 정렬된 output을 받아내는 것.

3) variable i used
build max heap을 구성하는 다양한 구조체를 선언했다.

<문제 2>
1)purpose of this code
문자열을 입력 받은 후 이것을 huffman code로 인코딩 한 후 다시 디코딩을 하는 것

2)input and output
input으로는 알파벳, 알파벳이 담긴 스트링, output으로는 encoding 된 코드와 decoding 된 문자열이다.

3) variable i used
배열, for 문을 위한 정수 배열 등 다양한 변수를 선언하였다.